node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
.svelte-kit/
/build

# OS
.DS_Store
Thumbs.db

# Env & secrets (never commit credentials)
.env
.env.*
!.env.example
!.env.test
.envrc
.direnv/
*.pem

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# SQLite write-ahead sidecars (not meant for version control; commit *.sqlite3 only)
**/*.sqlite3-wal
**/*.sqlite3-shm

# Local git worktrees (used for branch comparisons)
.worktrees/

# Editor / AI tooling (machine-local; keep out of GitHub)
.cursor/
.vscode/
.idea/
*.code-workspace

# Claude Code — local/session state
.claude/
.Claude/
